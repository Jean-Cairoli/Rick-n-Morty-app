import { Box, Typography } from '@mui/material';

const Header = () => {
	return (
		<Box className="container container-header backgund">
			<Typography variant="h1">FANDOM WIKI</Typography>
		</Box>
	);
};

export default Header;
